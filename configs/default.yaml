device: "cpu"
data:
  sizes: [8, 16, 32, 64, 128]
  types: ["random", "sparse", "low_rank"]
  train_samples: 100000
  val_samples: 5000
  batch_size: 256
  num_workers: 4
training:
  epochs: 50
  learning_rate: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 10
  log_interval: 50
